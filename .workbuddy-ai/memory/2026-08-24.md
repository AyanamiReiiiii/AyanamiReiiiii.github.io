# 2026-08-24

## Hilbert loader removed & archived
- User no longer wants the loading animation. Removed overlay + script from index.html; body now has class="no-loader" so the page shows instantly (main fade-in CSS rule for .no-loader covers this).
- Archived the full animation code at archive/hilbert-loader.html: re-enable instructions (3 steps), the exact overlay HTML + script to paste back, knobs to tweak (order, 2s draw, bounce), plus a live self-contained demo. #loader CSS kept in style.css so re-enabling needs no style changes.
- User's bio line changed to "I'm a student studying in US" (they edited it themselves).
