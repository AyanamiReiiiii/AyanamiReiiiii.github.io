# 2026-08-20

## Site iteration 2 (user feedback)
- Loader: order-4 Hilbert curve, black (#222), much smaller (min(26vmin,160px)), added button-press bounce (scale 0.88 → 1) after the 2s draw, before fade-out.
- math.html: removed standalone Möbius blurb section; writings list = WTF is Riemann Surface? (→ Math_Writings/Riemann_Galois.pdf, desc "Explained in one minute") + Möbius PDF (with desc). Removed Cauchy + Short Path entries.
- Description style: `.item` + `.desc` — gray, 0.76rem, under the title link.
- cs.html + thoughts.html: same writings-list layout as math page; entries link to blog pages in posts/.
- Created posts/ dir with 3 blog pages (why-i-hand-write-my-website, on-pre-college-math-education, why-write-exposition) — no footer icons on blog pages, back-link instead.
- Footer on all main pages: GitHub + LinkedIn icons (github.com/AyanamiReiiiii, linkedin.com/in/yifan-mo-37056a388).
- Added .gitignore (.DS_Store). NOT committed/pushed — user pushes manually.

## Important notes
- Live site = GitHub Pages serving `main` branch; work happens on `new-design` branch. User must merge new-design → main for changes to go live.
- User shared a PAT in chat — advised revoking it; never commit it.
- User workflow: edits locally, pushes manually; wants AI to make edits but handles git himself (for now).

## Writing system (turn 3)
- Auto-indexed writings: `writings.js` (pure JSON data manifest) + `list.js` (renderWritings helper). index (all, limit 8), math/cs/thoughts pages render lists from it, sorted date desc. PDFs open in new tab.
- Folder restructure: `math/` (PDFs), `cs/`, `thoughts/` (HTML posts, date-prefixed filenames). Math_Writings/ and posts/ removed.
- Local writing tool: `tools/publish.py` (stdlib-only Python server, port 8844) + `tools/editor.html` (form + markdown live preview with own mini converter, $LaTeX$ via MathJax, image drag-drop upload). Publish → writes `<category>/YYYY-MM-DD-slug.html` (+.md source), updates writings.js. Test-verified via curl, artifacts cleaned.
- User explicitly does NOT want AI to convert notes → HTML; they use the tool instead.

## Writing tool v2 (turn 4)
- Fixed preview image bug: editor preview now rewrites `src="images/..."` → `src="/<category>/images/..."` before rendering (editor page lives at site root, images live in category folder).
- Publish feedback: green banner "✓ Published to <path> — view the page" on success, ✗ message on failure.
- Title box = search: dropdown of existing writings in selected category (word-substring match, arrow keys + Enter supported). Selecting one loads its .md source into the editor (badge "Editing an existing writing"), button switches to "Update", publish overwrites that file and updates its writings.js entry in place (no duplicate).
- Exact title match against a DIFFERENT writing in same category → red warning + publish button disabled.
- New server endpoints: GET /writings (manifest), GET /source?file= (front-matter + markdown); /publish accepts optional "overwrite".
- Created .md sources for the 3 pre-tool posts so they are editable via the tool.

## LESSON — data loss mistake
- While testing the overwrite feature I overwrote the USER'S REAL post "Agents are amazing" (cs/2026-08-12-agents-are-amazing.html/.md) with placeholder content. It was untracked in git → unrecoverable. NEVER test destructive flows on real user content; always create a throwaway post, test, then delete it. Told the user; they can republish from their notes via the tool's load-edit feature.
