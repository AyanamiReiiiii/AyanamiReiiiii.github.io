# 2026-08-21

## Writing tool fixes (turn 5)
- Dropdown bug: clicking a PDF entry failed silently (no .md source). Fix: PDFs excluded from title-search dropdown; load errors now shown in the red warning area under the title box instead of the tiny status line.
- Exit message changed from "ready to commit" to concrete VS Code steps (stage all → message → Commit → ... → Push; live site = main branch).
- User is migrating to VS Code Source Control for git; wants conceptual git explanations tied to the GUI. Configured git identity question answered (user.name/user.email).

## Root cause of "title search not working"
- User was opening the editor via the WorkBuddy static preview mirror (127.0.0.1:55387), which serves editor.html but has NO backend (/writings 403). So dropdown/publish silently failed. Correct address: http://127.0.0.1:8844/ (auto-opened by tools/publish.py).
- Fix: editor now detects missing backend on load and shows red warning telling user to use 127.0.0.1:8844. NOTE: never present_files the raw editor.html for this user — only the live 8844 URL.
